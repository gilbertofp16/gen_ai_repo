Follow all rules in `.claude/rules/`.

Language selection:
- Apply `python.md` for Python code.
- Apply `typescript-node.md` for TypeScript, JavaScript, and Node.js code.

General behavior:
- Inspect existing code before proposing changes.
- Reuse existing patterns and utilities where appropriate.
- Do not invent files, functions, imports, or dependencies.
- For non-trivial changes, include tests.